import React, { Component } from "react";
import "./logo.scss";

class Logo extends Component {
    render() {
        return (
                    <div className="site-title"></div>
        );
    }
}

export default (Logo);
